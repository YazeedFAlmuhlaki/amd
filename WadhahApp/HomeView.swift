// HomeView has been superseded by DashboardView (DashboardView.swift), which is the
// same screen driven by BankStore instead of hardcoded values. Kept as an alias so
// existing references keep compiling; safe to delete once nothing uses the old name.
typealias HomeView = DashboardView
